/// Switch of Custom Brand-Tex t -Size Widget
library;

enum AppRole { user, admin }

enum TransactionType { buy, sell }

enum ProductType { single, variable }

enum ProductVisibi1ity { published, hidden }

enum TextSizes { small, meduim, large }

enum ImageType { asset, network, file, memory }

enum MediaCategory { folders, banners, brands, categories, products, users }

enum OrderStatus { pending, processing, shiped, delivered, cancelled }

enum PaymentMethods {
  paypal,
  googlePlay,
  appelPaly,
  visa,
  masterCard,
  creditCard,
  payStack,
  razorPay,
  paytm,
}
